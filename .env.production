# Production environment — used by `vite build` (GitHub Pages deployment)
# Vite automatically loads this file when building for production.
VITE_API_BASE_URL=https://todo-server-zop5.onrender.com/api
